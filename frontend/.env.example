# Variables del frontend (Vite expone solo las que empiezan con VITE_).
# Copiá este archivo como .env y completá los valores.

# API key de Google Maps JavaScript API (restringida por referer HTTP).
VITE_GOOGLE_MAPS_API_KEY=
